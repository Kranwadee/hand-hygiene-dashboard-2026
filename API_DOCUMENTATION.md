# 📋 API Documentation - Hand Hygiene System

## 🔗 สำหรับเพื่อนที่ทำเว็บแอพถ่าย UV

เอกสารนี้อธิบายวิธีส่งข้อมูลจากเว็บแอพถ่าย UV มายัง Dashboard

---

## 📊 ภาพรวมระบบ

```
┌─────────────────────────┐          ┌─────────────────────────┐
│  📱 เว็บแอพถ่าย UV       │          │  📊 Dashboard แสดงผล     │
│      (เพื่อนทำ)          │          │      (ฉันทำ)             │
└───────────┬─────────────┘          └───────────┬─────────────┘
            │                                    │
            │  POST ส่งข้อมูล                     │  GET ดึงข้อมูล
            ▼                                    ▼
      ┌─────────────────────────────────────────────────┐
      │            🗄️ Database (ตรงกลาง)               │
      │         เช่น Supabase, Firebase, etc.          │
      └─────────────────────────────────────────────────┘
```

---

## 📡 API Endpoints

### Mock API (สำหรับทดสอบ)
- **Base URL:** `http://localhost:3000`
- **GET** `/api/scan-logs` - ดึงข้อมูลทั้งหมด
- **GET** `/api/employees` - ดึงข้อมูลพนักงานทั้งหมด
- **GET** `/api/employees/:id` - ดึงข้อมูลพนักงานตาม ID

---

## 📝 รูปแบบข้อมูล (JSON Schema)

เมื่อบันทึกผลสแกน ต้องส่งข้อมูลในรูปแบบนี้:

```json
{
  "Timestamp": "2026-02-06T10:30:00",
  "EmployeeID": "620913",
  "EmployeeName": "สมชาย ใจดี",
  "Department": "ICU",
  "Position": "พยาบาล",
  "Hospital": "BDMS Phuket",
  "Age": 28,
  "Tenure": 3,
  "FrontScore": 86.97,
  "FrontSpots": 70,
  "FrontDirtyZones": "นิ้วโป้ง,ข้อมือ",
  "BackScore": 93.69,
  "BackSpots": 15,
  "BackDirtyZones": "-",
  "Status": "PASS",
  "ActivityName": "กิจกรรมล้างมือ"
}
```

---

## 📋 คำอธิบาย Fields

| Field | Type | คำอธิบาย | ตัวอย่าง |
|-------|------|---------|---------|
| `Timestamp` | string | เวลาที่สแกน (ISO 8601) | `2026-02-06T10:30:00` |
| `EmployeeID` | string | รหัสพนักงาน 6 หลัก | `620913` |
| `EmployeeName` | string | ชื่อ-นามสกุล | `สมชาย ใจดี` |
| `Department` | string | แผนก | `ICU`, `ER`, `OPD`, `Ward A`, `OR` |
| `Position` | string | ตำแหน่ง | `แพทย์`, `พยาบาล`, `เจ้าหน้าที่` |
| `Hospital` | string | โรงพยาบาล | `BDMS Phuket` |
| `Age` | number | อายุจริง (ปี) | `28` |
| `Tenure` | number | อายุงาน (ปี) | `3` |
| `FrontScore` | number | คะแนนฝ่ามือ (0-100%) | `86.97` |
| `FrontSpots` | number | จำนวนจุดสกปรกบนฝ่ามือ | `70` |
| `FrontDirtyZones` | string | โซนที่สกปรกบนฝ่ามือ | `นิ้วโป้ง,ข้อมือ` หรือ `-` |
| `BackScore` | number | คะแนนหลังมือ (0-100%) | `93.69` |
| `BackSpots` | number | จำนวนจุดสกปรกบนหลังมือ | `15` |
| `BackDirtyZones` | string | โซนที่สกปรกบนหลังมือ | `นิ้วกลาง` หรือ `-` |
| `Status` | string | ผลการตรวจ | `PASS` หรือ `WARNING` |
| `ActivityName` | string | ชื่อกิจกรรม | `กิจกรรมล้างมือประจำวัน` |

---

## 🎯 รายชื่อโซน (Dirty Zones)

ใช้ชื่อภาษาไทยต่อไปนี้:

| Zone | ชื่อไทย |
|------|--------|
| Thumb | นิ้วโป้ง |
| Index | นิ้วชี้ |
| Middle | นิ้วกลาง |
| Ring | นิ้วนาง |
| Pinky | นิ้วก้อย |
| Palm | ฝ่ามือ |
| Back | หลังมือ |
| Wrist | ข้อมือ |
| Gaps | ซอกนิ้ว |
| Nails | เล็บ |

**หมายเหตุ:** ถ้าไม่มีโซนสกปรก ให้ใส่ `-`

---

## ✅ กฎการตัดสิน Status

```
ถ้า (FrontScore + BackScore) / 2 >= 80
  → Status = "PASS"
มิฉะนั้น
  → Status = "WARNING"
```

---

## 💻 ตัวอย่างการส่งข้อมูล (JavaScript)

```javascript
const scanResult = {
  Timestamp: new Date().toISOString(),
  EmployeeID: "620913",
  EmployeeName: "สมชาย ใจดี",
  Department: "ICU",
  Position: "พยาบาล",
  Hospital: "BDMS Phuket",
  Age: 28,
  Tenure: 3,
  FrontScore: 86.97,
  FrontSpots: 70,
  FrontDirtyZones: "นิ้วโป้ง,ข้อมือ",
  BackScore: 93.69,
  BackSpots: 15,
  BackDirtyZones: "-",
  Status: "PASS",
  ActivityName: "กิจกรรมล้างมือ"
};

// ส่งไป Supabase (ตัวอย่าง)
const { data, error } = await supabase
  .from('scan_logs')
  .insert([scanResult]);
```

---

## 🗄️ Database ที่แนะนำ

### Supabase (แนะนำ!)
- ฟรี
- ใช้ง่าย
- มี SDK สำหรับ JavaScript, Python, React
- รองรับ Real-time

### วิธีสร้าง Table
```sql
CREATE TABLE scan_logs (
  id SERIAL PRIMARY KEY,
  timestamp TIMESTAMPTZ NOT NULL,
  employee_id VARCHAR(10) NOT NULL,
  employee_name VARCHAR(100),
  department VARCHAR(50),
  position VARCHAR(50),
  hospital VARCHAR(100),
  age INTEGER,
  tenure INTEGER,
  front_score DECIMAL(5,2),
  front_spots INTEGER,
  front_dirty_zones TEXT,
  back_score DECIMAL(5,2),
  back_spots INTEGER,
  back_dirty_zones TEXT,
  status VARCHAR(10),
  activity_name VARCHAR(100),
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 📞 ติดต่อ

มีคำถามหรือต้องการแก้ไข Schema ติดต่อได้เลย!

---

*อัปเดตล่าสุด: 2026-02-06*
